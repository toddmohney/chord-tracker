services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: chord_tracker
      POSTGRES_PASSWORD: chord_tracker
      POSTGRES_DB: chord_tracker
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
